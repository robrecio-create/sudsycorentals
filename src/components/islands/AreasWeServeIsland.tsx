import ClientApp from "@/components/ClientApp";
import AreasWeServe from "@/views/AreasWeServe";

export default function AreasWeServeIsland() {
  return (
    <ClientApp>
      <AreasWeServe />
    </ClientApp>
  );
}
